---
- name: Deploy node1 API service
  hosts: node1
  become: true
  gather_facts: true
  vars:
    api_app_dir: /opt/node1_api
    api_venv_path: /opt/mini-orch-venv
    api_service_name: node1-api
    api_data_dir: /var/lib/node1_api
    sqlite_db_path: /var/lib/node1_api/jobs.db
    sqlite_backup_dir: /var/backups/node1_api
    redis_password: "IntraNet-Redis-2026!ChangeMe"
    redis_url: "{{ 'redis://:' ~ redis_password ~ '@127.0.0.1:6379/0' if (redis_password | length > 0) else 'redis://127.0.0.1:6379/0' }}"

  tasks:
    - name: Ensure SQLite backup directory exists
      ansible.builtin.file:
        path: "{{ sqlite_backup_dir }}"
        state: directory
        owner: root
        group: root
        mode: "0755"

    - name: Check existing SQLite database
      ansible.builtin.stat:
        path: "{{ sqlite_db_path }}"
      register: sqlite_db_stat

    - name: Backup SQLite database before redeploy
      ansible.builtin.copy:
        src: "{{ sqlite_db_path }}"
        dest: "{{ sqlite_backup_dir }}/jobs-{{ ansible_date_time.iso8601_basic_short }}.db"
        remote_src: true
        owner: root
        group: root
        mode: "0640"
      when: sqlite_db_stat.stat.exists

    - name: Remove API app directory for clean redeploy
      ansible.builtin.file:
        path: "{{ api_app_dir }}"
        state: absent

    - name: Ensure API app directory exists
      ansible.builtin.file:
        path: "{{ api_app_dir }}"
        state: directory
        owner: root
        group: root
        mode: "0755"

    - name: Upload Flask app
      ansible.builtin.copy:
        src: ../node1_api/app.py
        dest: "{{ api_app_dir }}/app.py"
        owner: root
        group: root
        mode: "0644"

    - name: Upload requirements
      ansible.builtin.copy:
        src: ../node1_api/requirements.txt
        dest: "{{ api_app_dir }}/requirements.txt"
        owner: root
        group: root
        mode: "0644"

    - name: Upload API test scripts
      ansible.builtin.copy:
        src: ../node1_api/tests/
        dest: "{{ api_app_dir }}/tests/"
        owner: root
        group: root
        mode: preserve

    - name: Ensure API venv exists
      ansible.builtin.command:
        cmd: "python3 -m venv {{ api_venv_path }}"
        creates: "{{ api_venv_path }}/bin/activate"

    - name: Install API dependencies into venv
      ansible.builtin.pip:
        requirements: "{{ api_app_dir }}/requirements.txt"
        virtualenv: "{{ api_venv_path }}"

    - name: Create API environment file
      ansible.builtin.copy:
        dest: /etc/node1_api.env
        owner: root
        group: root
        mode: "0644"
        content: |
          JOB_DB_PATH=/var/lib/node1_api/jobs.db
          REDIS_URL={{ redis_url }}
          QUEUE_STREAM_KEY=jobs:stream

    - name: Remove API data directory for clean redeploy
      ansible.builtin.file:
        path: "{{ api_data_dir }}"
        state: absent

    - name: Ensure API data directory exists
      ansible.builtin.file:
        path: "{{ api_data_dir }}"
        state: directory
        owner: root
        group: root
        mode: "0755"

    - name: Create systemd service for node1 API
      ansible.builtin.copy:
        dest: "/etc/systemd/system/{{ api_service_name }}.service"
        owner: root
        group: root
        mode: "0644"
        content: |
          [Unit]
          Description=Node1 Flask API Service
          After=network.target redis-server.service

          [Service]
          Type=simple
          EnvironmentFile=/etc/node1_api.env
          WorkingDirectory={{ api_app_dir }}
          ExecStart={{ api_venv_path }}/bin/python {{ api_app_dir }}/app.py
          Restart=always
          RestartSec=3

          [Install]
          WantedBy=multi-user.target
      notify: Restart node1 API

    - name: Ensure node1 API service enabled and started
      ansible.builtin.systemd:
        name: "{{ api_service_name }}"
        enabled: true
        state: started
        daemon_reload: true

  handlers:
    - name: Restart node1 API
      ansible.builtin.systemd:
        name: "{{ api_service_name }}"
        state: restarted
        daemon_reload: true

- name: Deploy node2 stream worker binary
  hosts: node2
  become: true
  gather_facts: true
  vars:
    worker_binary_src: ../node2_worker/bin/node2-worker
    worker_binary_dest: /usr/local/bin/node2-worker
    worker_service_name: node2-worker
    redis_password: "IntraNet-Redis-2026!ChangeMe"
    node1_redis_host: "{{ hostvars['node1']['redis_private_ip'] | default(hostvars['node1']['ansible_host'] | default('127.0.0.1')) }}"
    redis_url: "{{ 'redis://:' ~ redis_password ~ '@' ~ node1_redis_host ~ ':6379/0' if (redis_password | length > 0) else 'redis://' ~ node1_redis_host ~ ':6379/0' }}"

  tasks:
    - name: Check worker binary exists on control node
      delegate_to: localhost
      become: false
      ansible.builtin.stat:
        path: "{{ worker_binary_src }}"
      register: worker_binary_stat

    - name: Fail if worker binary is missing
      delegate_to: localhost
      become: false
      ansible.builtin.fail:
        msg: "Missing worker binary: {{ worker_binary_src }}. Build it first: ./node2_worker/build.sh"
      when: not worker_binary_stat.stat.exists

    - name: Upload node2 worker binary
      ansible.builtin.copy:
        src: "{{ worker_binary_src }}"
        dest: "{{ worker_binary_dest }}"
        owner: root
        group: root
        mode: "0755"

    - name: Create node2 worker environment file
      ansible.builtin.copy:
        dest: /etc/node2_worker.env
        owner: root
        group: root
        mode: "0644"
        content: |
          REDIS_URL={{ redis_url }}
          QUEUE_STREAM_KEY=jobs:stream
          QUEUE_GROUP=node2-workers
          QUEUE_CONSUMER={{ inventory_hostname }}
          QUEUE_BATCH_SIZE=10
          QUEUE_BLOCK_MS=5000

    - name: Create systemd service for node2 worker
      ansible.builtin.copy:
        dest: "/etc/systemd/system/{{ worker_service_name }}.service"
        owner: root
        group: root
        mode: "0644"
        content: |
          [Unit]
          Description=Node2 Redis Stream Worker
          After=network.target redis-server.service

          [Service]
          Type=simple
          EnvironmentFile=/etc/node2_worker.env
          ExecStart={{ worker_binary_dest }}
          Restart=always
          RestartSec=3

          [Install]
          WantedBy=multi-user.target
      notify: Restart node2 worker

    - name: Ensure node2 worker service enabled and started
      ansible.builtin.systemd:
        name: "{{ worker_service_name }}"
        enabled: true
        state: started
        daemon_reload: true

  handlers:
    - name: Restart node2 worker
      ansible.builtin.systemd:
        name: "{{ worker_service_name }}"
        state: restarted
        daemon_reload: true
